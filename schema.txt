User{
	userid -String,
	uname -String,
	upassword -hashString,
	contact -String (phone),
	uemail -String,
	profilepic -Image,
	bookemarked - [Book] (array of Books),
	cart -[Book] (array of Books)
}

Book{
	bookid -String,
	bookname -String,
	bookdescription -String,
	bookimage -Image,
	category -String,
	price -Number,
	type -String (rent/sell),
	duration -String (if rent),
	author -String,
	publisher -String,
	edition -String,
	userid -String,
	requestcount -Number,
	issold -boolean,
	postdate -Date,
	requests -[Request] (array of Requests)
}

Question{
	questionid -String,
	title -String,
	description -String,
	userid -String,
	answerscount -Number,
	answers -[Answer] (array of Answers),
	askdate -Date,
	isClosed -boolean,
	category -String
}

Answer{
	answerid -String,
	questionid -String,
	answer -String,
	userid -String,
	answerdate -Date,
	hearts -Number
}

Demo{
	demoid -String,
	title -String,
	description -String,
	file -File,
	userid -String,
	starcount -Number,
	ratedcount -Number,
	reviewscount -Number,
	reviews -[Review] (Array of Reviews),
	demodate -Date
}

Review{
	reviewid -String,
	review -String,
	userid -String,
	reviewdate -Date
}

Request{
	requestid -String,
	bookid -String,
	userid -String,
	requestdate -Date
}